language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - python setup.py install
  - pip install codecov
  - pip install pytest-cov
script:
  - pytest --cov=opendap_protocol --cov-report term
after_success:
  - codecov